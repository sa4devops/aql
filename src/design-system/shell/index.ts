/**
 * Design System — Shell Reference
 * Status: VISUAL BASELINE CANDIDATE — NOT GOVERNING — NOT YET APPROVED
 *
 * Re-exports the existing AppShell and its context for use in design-system imports.
 * The App Shell is NOT rebuilt here — it is referenced from its canonical location.
 *
 * Canonical location: src/components/AppShell.tsx
 */

export { default as AppShell, AppContext, useApp } from '@/components/AppShell';
export type { } from '@/components/AppShell';
